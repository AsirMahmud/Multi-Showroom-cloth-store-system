# Required during the production build. Include the backend /api path.
NEXT_PUBLIC_API_URL=https://your-backend.example.com/api

# Optional. Media URLs are derived from NEXT_PUBLIC_API_URL when omitted.
# NEXT_PUBLIC_IMAGE_URL=https://your-backend.example.com
